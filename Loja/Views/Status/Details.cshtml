@model Loja.Models.Status

@{
    ViewBag.Title = "Details";
}
<div class="container">
    <h2>Detalhes</h2>

    <div>
        <h4>Status</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.NomeStatus)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NomeStatus)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Editar", "Edit", new { id = Model.id }) |
        @Html.ActionLink("Voltar para Listagem", "Index")
    </p>
</div>